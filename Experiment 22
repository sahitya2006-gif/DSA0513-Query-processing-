# DataFrame with black background and yellow font
import pandas as pd
import numpy as np

df = pd.DataFrame(np.random.randn(10,4), columns=['A','B','C','D'])

styled = df.style.set_properties(**{
    'background-color':'black',
    'color':'yellow'
})

print(styled)
