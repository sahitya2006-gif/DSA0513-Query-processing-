import pandas as pd

data = {
'Item':['Pen','Pen','Pencil','Pencil'],
'Sales':[200,300,150,100]
}

df = pd.DataFrame(data)

pivot = pd.pivot_table(df, values='Sales', index='Item', aggfunc=['max','min'])

print(pivot)
