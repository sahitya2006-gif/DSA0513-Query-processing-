# Pivot table to find maximum and minimum sale value
import pandas as pd

df = pd.read_csv("sales_data.csv")

pivot = pd.pivot_table(df, values='Sale_amt', index='Item',
                       aggfunc=['max','min'])

print(pivot)
